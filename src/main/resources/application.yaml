spring:
  application:
    name: capability-mesh
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          temperature: 0.7
          max-tokens: 2048
          model: "claude-opus-4-6"
agent:
  skills:
    dirs: "classpath:/skills"
